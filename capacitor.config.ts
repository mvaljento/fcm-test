import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.unitt.messenger',
  appName: 'app',
  webDir: 'dist/app',
  bundledWebRuntime: false
};

export default config;
